import { createStitches } from '@stitches/react';

export const { styled, css, globalCss } = createStitches({
  theme: {
    colors: {
      primary: '#b892ff',
      secundary: '#ff4c52',
      tertiary: '#38e4ae',

      background: '#0b0014',
      text: '#fdfffc',
    },
  },
});
