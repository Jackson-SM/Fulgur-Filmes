import { createTheme } from '../../config/stitches.config';

export const lightTheme = createTheme({
  colors: {
    primary: '#715aff',
    secundary: '#ff4c52',
    tertiary: '#7CC5F3',

    background: '#f3f3f3',
    text: '#131313',
  },
});
