import { styled } from '../../config/stitches.config';

export const StyledForm = styled('form', {
  display: 'flex',
  flexDirection: 'column',
  alignItems: 'center',
  gap: '10px',

  padding: '40px',

  flex: '1',
});
